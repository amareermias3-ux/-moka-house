/* ================================================================
   MOKA HOUSE / ERSCOMAS — Vercel entry point
   Runs the root server.js (single source of truth).
   Do NOT put a copy of the server here — always import it.
================================================================ */
import server from '../server.js';
export default server;